# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.venv/
venv/
env/

# Node
node_modules/
dist/
build/
*.log

# Environment
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker volumes
pgdata/
ollama_data/

# Dropzone files
dropzone/*.835
dropzone/*.txt

# ── Added 2026-09-05 ──

# Local working backups taken during development
*.bak
*.bak.*
*.pre-fix
*.pre-rewrite
*.pre-init
*.pre-colfix
*.invalid

# Parsed EDI output contains claim and patient data. The directory itself is a
# bind mount that must exist, so keep it and ignore its contents.
ediparser/output/
# (Docker recreates this bind mount on `docker compose up`.)

# Database dumps contain claims and denials, including patient names and dates
# of birth. Never commit these.
database/backups/

# Dropzone holds uploaded remittances
ediparser/dropzone/
dropzone/

# Superseded files kept out of the way rather than deleted.
.attic/

# ── Rust rewrite (branch rewrite/rust-ts) ──
/target/

# Large reference-data import samples and scratch remittances used during the
# rewrite; test inputs, not source.
/icd10_october2026_0.csv
/.835cg.pdf
/.835cg.txt
/.835fields.tmp
/.fcso835.pdf
/.fcso835.txt
/.hdi835.dat
/.uhc835.pdf
/.uhc835.txt
/.tree.json

# Local Rust-stack test rig (parallel to the Python compose stack)
/.rust-stack/

# Rust compose stack self-signed certs
/certs-rust/

# editor timestamped backups
*.bak-*
