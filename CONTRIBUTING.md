# Contributing

Use synthetic data only. Do not commit PHI, production credentials, or raw EDI
files. Run `just test-rust` and `just test-web`; changes to trust boundaries,
storage, or deployment require an ADR.
